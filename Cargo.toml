[package]
name = "zoll"
version = "0.3.0"
edition = "2024"
description = "Zoll markup language — lazy, line-level parser"

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
pulldown-cmark = "0.13"

[[bench]]
name = "compare"
harness = false

[profile.bench]
debug = 1
