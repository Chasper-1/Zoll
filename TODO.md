# TODO — Zoll (парсер/движок)

## Приоритет: производительность

- [ ] `dependency.rs` — бинарный поиск в `spans_in`/`spans_overlapping` (спаны не отсортированы по start — блочные ломают порядок; нужен secondary sorted index)
- [x] `simd.rs` — lookup-таблица 256 байт для скалярного фолбэка вместо `targets.contains()` O(n)
- [ ] `parser.rs` — generic-версии `parse_into`/`reparse_into` для мономорфизации (убрать `dyn SpanSink` из горячего пути)

## Приоритет: читаемость

- [x] `parser.rs` — вынести логику мержа маркеров в `MarkerAccumulator`
- [x] `resolver.rs` — убрать `text: &[u8]` из `push_inline`
- [x] `line_map.rs` — комментарий к `line_at`

## Приоритет: тесты

- [x] Тест: длинная строка со множеством маркеров (150 inline)
- [x] Тест: документ целиком из маркеров (0 обычного текста)
- [x] Тест: пустой документ через `parse_into`
- [x] Тест: стрим-режим — порядок спанов на границах блочных конструкций

## Не реализовано (из syntax.md)

- [ ] Экранирование `\`
- [ ] Инлайн-код (обратные кавычки)
- [ ] Парные скобки: `[[ ]]`, `[ ]( )`, `[~ ]`/`{~ }`, `[$ ]`/`{$ }`, `{: }`
- [ ] Callout-блоки `[! ]`
- [ ] Таблицы с выравниванием (колоночное + по ячейкам)
- [ ] Инкрементальный переразбор
- [ ] Параллельный второй проход
