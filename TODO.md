# TODO — Zoll (парсер/движок)

## Приоритет: производительность

- [ ] `dependency.rs` — бинарный поиск в `spans_in`/`spans_overlapping` вместо линейного прохода (спаны отсортированы)
- [ ] `simd.rs` — lookup-таблица 256 байт для скалярного фолбэка вместо `targets.contains()` O(n)
- [ ] `parser.rs` — generic-версии `parse_into`/`reparse_into` для мономорфизации (убрать `dyn SpanSink` из горячего пути)

## Приоритет: читаемость

- [ ] `parser.rs` — вынести логику мержа маркеров из замыкания scan в `MarkerAccumulator`
- [ ] `resolver.rs` — убрать `text: &[u8]` из `push_inline`, брать из `state.text`
- [ ] `line_map.rs` — добавить комментарий к `line_at` почему `Ok(index)` корректен

## Приоритет: тесты

- [ ] Тест: длинная строка со множеством маркеров (>100)
- [ ] Тест: документ целиком из маркеров (0 обычного текста)
- [ ] Тест: пустой документ через `parse_into`
- [ ] Тест: стрим-режим — порядок спанов на границах блочных конструкций

## Не реализовано (из syntax.md)

- [ ] Экранирование `\`
- [ ] Инлайн-код (обратные кавычки)
- [ ] Парные скобки: `[[ ]]`, `[ ]( )`, `[~ ]`/`{~ }`, `[$ ]`/`{$ }`, `{: }`
- [ ] Callout-блоки `[! ]`
- [ ] Таблицы с выравниванием (колоночное + по ячейкам)
- [ ] Инкрементальный переразбор (пересобирает весь документ)
- [ ] Параллельный второй проход
